# Accio Manager

Accio Manager 是一个本地运行的 Accio 多账号管理面板，提供账号资料查看、凭证保存、配额追踪、活跃账号识别以及本地 Profile 切换能力，适合个人在本机环境中管理和维护多个 Accio 使用身份。

## 项目简介

这个项目基于 `Node.js + Express` 构建，前端为静态页面，后端负责读取本地运行环境中的账号信息、Accio 应用状态和缓存数据，并通过 Web 面板统一展示。

当前项目重点在于：

- 汇总本地账号列表与活跃账号状态
- 保存账号本地 Profile 与认证文件
- 展示账号配额、认证有效期与基础元数据
- 提供本地切换和 OAuth 切换相关能力
- 将敏感运行数据保存在本地 `data/` 目录，避免直接进入版本库

## 功能概览

- 多账号总览：展示账号数量、任务数量、会话数量等统计信息
- 活跃账号检测：结合本地日志与元数据识别当前账号
- 凭证管理：保存并展示 Token、Cookie、设备信息等认证数据
- Profile 管理：按账号保存本地 Profile 快照，支持重复切换
- 配额跟踪：记录账号配额消耗、刷新倒计时和检查时间
- 本地面板：通过浏览器访问本机服务进行操作

## 技术结构

- 后端入口：`server.js`
- 前端页面：`public/index.html`
- 前端逻辑：`public/app.js`
- 前端样式：`public/styles.css`
- 本地运行数据：`data/`

## 快速开始

### 环境要求

- Node.js 18 及以上
- macOS 本地环境
- 已安装并使用过 Accio 桌面应用

### 安装依赖

```bash
npm install
```

### 启动项目

```bash
npm run dev
```

启动后访问：

```text
http://localhost:3456
```

## 本地数据说明

应用运行时会在项目根目录创建或使用 `data/` 目录，常见内容包括：

- `data/accounts_meta.json`：账号元信息、认证信息、配额信息
- `data/profiles/`：每个账号独立保存的本地 Profile、Cookie、凭证文件

同时，程序还会读取用户目录中的 Accio 本地数据，例如：

- `~/.accio/accounts`
- `~/.accio/settings.jsonc`
- `~/.accio/logs/sdk.log`
- `~/Library/Application Support/Accio`

这些文件可能包含敏感账号信息、登录态、设备标识或本地缓存，默认仅建议在个人本机环境中使用。

## Git 忽略规则

仓库默认忽略以下本地文件，避免误提交：

- `data/`
- `node_modules/`
- `.env*`
- `config/*.local.*`
- `config/*.secret.*`
- `config/private/`
- `logs/`

## 使用建议

- 仅在你可控的本机环境中运行
- 不要将 `data/`、日志文件或本地配置上传到公开仓库
- 不要在不受信任的机器上复制本地凭证文件
- 提交代码前先检查 Git 暂存区，确认没有敏感信息

## 免责声明

本项目为非官方开源工具，仅用于学习、研究和个人效率提升目的。

- 本项目与 Accio 及其关联公司不存在官方隶属、授权、担保或背书关系
- 项目中涉及的第三方产品名称、商标、服务名称及品牌归其各自权利人所有
- 使用者应自行确认其操作符合所在地区法律法规、平台条款、服务协议及内部合规要求
- 因使用本项目导致的账号风险、数据丢失、权限异常、封禁、服务中断或其他任何损失，项目作者与贡献者不承担责任
- 本项目不保证对第三方服务的兼容性、持续可用性或未来行为一致性

如果你计划将本项目用于团队环境、生产环境或商业用途，建议先进行完整的安全审查、合规评估和内部授权确认。

## 开源协议

本项目采用 `MIT License` 开源发布，详细内容见 [LICENSE](./LICENSE)。

你可以自由使用、修改、分发本项目代码，但需保留原始版权和许可声明。项目按“现状”提供，不附带任何明示或默示担保。

## 发布前检查

在公开发布仓库前，建议至少确认以下事项：

- `data/`、`node_modules/`、`.env*` 等本地文件没有被加入 Git
- 没有把真实账号、Token、Cookie、日志或截图提交到历史记录
- README 中的功能描述与当前代码行为一致
- `LICENSE`、免责声明和安全说明已经随仓库一并发布
- 如果要接受外部贡献，建议同时补充 Issue 模板和贡献说明

可以用下面的命令做最后检查：

```bash
git status --short --ignored
git ls-files
```


